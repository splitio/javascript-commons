import { SYNC_OFFLINE_LB, WARN_3 } from '../constants';

export const codesWarnNode: [number, string][] = [
  [WARN_3, SYNC_OFFLINE_LB + '.split mocks will be deprecated soon in favor of YAML files, which provide more targeting power. Take a look in our documentation.'],
];
